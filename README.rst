================
Todo application
================

Make virtualenv
===============

::

   $ virtualenv venv --python=python3.5
   $ . venv/bin/activate
   (venv)$ pip install -r requirements.txt

runserver
=========

::

   (venv)$ cd myproject
   (venv)$ python manage.py runserver
